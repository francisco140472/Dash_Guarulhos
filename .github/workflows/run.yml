name: Deploy no Streamlit

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v3

      - name: Sincronizar com Streamlit
        run: |
          curl -X POST https://share.streamlit.io/deploy --header "Authorization: Bearer ${{ secrets.STREAMLIT_TOKEN }}"
