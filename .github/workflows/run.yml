name: Deploy via FTP

on:
  push:
    branches:
      - main  # Altere se necessário

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v3

      - name: Upload para o servidor FTP
        uses: SamKirkland/FTP-Deploy-Action@v4
        with:
          server: ${{ secrets.200.98.80.97 }}
          username: ${{ secrets.ftp_jot }}
          password: ${{ secrets.SantoAndre2020 }}
          local-dir: "C:/Users/franc/OneDrive/Documentos/GitHub/Dash_Guarulhos"
          server-dir: H:/ ftp_jot
